/********************************************************************
	created:	Thursday 2026/07/16 at 09:00
	filename: 	RenderCaps.h
	author:		steffen.roemer
	notice:		This source file is part of orkige (orkitec Game engine)
				For the latest info, see http://www.orkitec.com/
	copyright:	(c) 2009-2026 orkitec
*********************************************************************/
#ifndef __RenderCaps_h__16_7_2026__09_00_00__
#define __RenderCaps_h__16_7_2026__09_00_00__

#include <core_util/String.h>

namespace Orkige
{
	//! @brief the render-capability vocabulary as an X-macro table - the SINGLE
	//! source of every capability's identity, name, kind and description. The
	//! enum, the name lookup and the parse all EXPAND from this one list, so
	//! they cannot drift from each other; there is no sidecar and no generated
	//! header. Per-FLAVOR values are deliberately NOT here: a backend fills its
	//! own capability bitset at boot (some caps are runtime-determined - e.g. a
	//! render-system pick), and that live fill is asserted against the committed
	//! snapshot tables (engine_render_classic/RenderCapsExpectedClassic.inc +
	//! engine_render_next/RenderCapsExpectedNext.inc), which also feed the
	//! GENERATED matrix in Docs/render-abstraction.md via Util/update_docs.py.
	//! Probe from code with RenderSystem::supports(RenderCaps::X), from Lua with
	//! engine:supports("name"), and over MCP from get_state's capabilities object.
	//!
	//!   X(Identifier, "name", Kind, "description")
	//!     Identifier    - the enum member (and the .inc snapshot key)
	//!     "name"        - the stable string (Lua / MCP / matrix column)
	//!     Kind          - Asymmetric (a real flavor delta) | PlannedAbsent
	//!                     (absent on BOTH flavors today - a v1 boundary,
	//!                     next-first when it lands)
	//!     "description" - the matrix "what it is" column
	//!
	//! Kind and description are consumed only by Util/update_docs.py (they are
	//! inert in C++ - no expansion below reads them); the runtime uses identity
	//! and name. Editing this table OR a snapshot .inc requires regenerating the
	//! matrix (python3 Util/update_docs.py --write; --check gates staleness).
#define ORKIGE_RENDER_CAPS(X) \
	X(SkyDome, "skyDome", Asymmetric, "a horizon-to-zenith sky dome behind the scene (sun-linked atmospheric on next; classic evaluates the same shared sky model per-pixel on GL3Plus/GLSL-ES3 targets with a vertex-colour gradient fallback on the GLES2/WebGL1/Vulkan floor) vs a flat clear colour; the dome is the `procedural` sky type - `AtmosphereDesc::skyType` also selects a cubemap `skybox` or a flat `colour` sky on both flavors") \
	X(DynamicShadows, "dynamicShadows", Asymmetric, "dynamic shadow maps cast by shadow-casting directional lights (next = compositor PSSM + PCF; classic = RTSS integrated PSSM folded into the one generated-material scheme - on GLES2/WebGL the bit is runtime-gated on depth-texture render targets)") \
	X(HemisphereAmbient, "hemisphereAmbient", Asymmetric, "a two-colour sky/ground ambient term evaluated PER PIXEL against the surface normal (mix(lower, upper, dot(hemisphereDir, N) * 0.5 + 0.5)); both flavors fill a surface's ambient from the same sky/ground split by the same formula, ambient x albedo - next natively (HlmsPbs ambient hemisphere, with that lane PINNED for every non-black fill so an EQUAL colour pair is not diverted to the backend's cheaper fixed-colour one, which multiplies the pi-divided reflectance and so answers a third of the brightness for the same authored fill), classic through a generated-shader hemisphere-ambient sub-render-state carried by every lit surface material, engine-generated and imported alike. A FLAT ambient is exactly the equal-hemisphere case on both (@see RenderWorld::setAmbientLight), verified within a flavor by the selfcheck's flat-versus-barely-unequal probe. Classic's fixed-function fallback (no shader generator) still sees one flat average") \
	X(SunExposureLinkage, "sunExposureLinkage", Asymmetric, "the atmosphere drives the linked sun's colour/power (an exposure the un-tonemapped pipeline can clip) - native on next, the same day/night curve evaluated on the CPU on classic (colour + per-pixel hemisphere ambient fill on the generated surface materials, tolerance parity)") \
	X(AnimatedNormalMappedWater, "animatedNormalMappedWater", Asymmetric, "fully animated normal-mapped water ripples; classic lights OR scrolls one normal map on a unit, not both, so its lit relief is static") \
	X(OffscreenOwnedLayers, "offscreenOwnedLayers", Asymmetric, "2D layers composited into an offscreen RenderTexture (the editor Preview panel .oui overlay + preview_ui), not just the main window") \
	X(ProjectedDecals, "projectedDecals", Asymmetric, "surface marks (impact/splat/footprint + blob-shadow fallback) as TRUE projected decals wrapping over geometry (next = HlmsPbs forward-clustered Decal) vs a surface-aligned textured quad floating above the surface (classic - flat, does not wrap uneven geometry)") \
	X(Bloom, "bloom", Asymmetric, "an LDR highlight-glow post-process on the 3D scene only (bright-pass -> separable blur -> additive combine, per-scene opt-in via engine:setBloom, the r.bloomQuality tier) - the 2D tier (sprites/vector shapes/gui) is excluded so UI stays crisp. next = CompositorManager2 quad passes inserted between the 3D scene pass and the 2D/UI pass; classic = the same chain as a viewport compositor over the generated-material scheme, the `OgreUnifiedShader.h` bright/blur/combine quad passes authored once and run in the GLSL ES 3.0 profile on a GLES/WebGL context - so it reaches the WebGL2/GLES3 web+device path too, gated on the glsl300es probe like the IBL stage; the GLES2/WebGL1 floor answers false and an enabled bloom degrades to no pass with one log line") \
	X(ScreenSpaceRefraction, "screenSpaceRefraction", Asymmetric, "opt-in screen-space refraction distortion through the water surface: the opaque scene colour is captured before the water draws and sampled at a normal-perturbed screen UV so what is under the water bends/wobbles (basic distortion, NOT depth-graded transmission). next = the HlmsPbs Refractive transparency mode fed by a compositor scene-colour+depth pass; classic = a grab-pass RenderTexture of the scene (water hidden) sampled at the perturbed screen UV, authored in two GLSL variants (desktop GL core + GLSL ES 3.0), so it reaches the WebGL2/GLES3 web+device path too - gated on the glsl300es probe like the IBL stage; the GLES2/WebGL1 floor keeps the byte-stable Stage-1 look, a Vulkan/Metal context answers false pending its own variant. BOTH flavors transmit the scene at its true LINEAR radiance, and each has to DECODE it to get there: neither capture is an sRGB surface, so the scene is written into it through the pipeline's sqrt display transfer, while both water paths accumulate in linear and encode once at the end - so the transmitted scene would otherwise be encoded TWICE and read roughly twice as bright wherever it is dark. next decodes with a full-screen quad into the float target its water pass refracts (the Orkige/Refraction/Linearize material on the refraction workspace's RefractRT pass); classic squares its display-space grab inside the water program. That shared convention is what lets the refraction lake's water band gate as a tolerance-parity corridor") \
	X(IblReflections, "iblReflections", Asymmetric, "opt-in image-based lighting sourced from the scene's SKY: specular reflections + a diffuse fill ADDED to the analytic lights on PBS-lit facade materials (next = the HlmsPbs reflection map + diffuse-GI env feature; classic = the engine-owned image-lighting sub-render-state over the same cubemap, evaluating next's live env term verbatim at the ONE shared intensity scale `IblPreset::fillScale` - on a GLES context the bit is runtime-gated on GLSL ES 3.0), tiered by the `r.iblQuality` cvar (`core_util/IblPreset.h`). ONE path, TWO sources selected automatically: a skybox atmosphere feeds the offline-baked prefiltered chain (`Util/make_sky_assets.py`); a procedural atmosphere feeds a runtime CPU capture of the sky (`core_util/SkyEnvMap` - a small cubemap synthesized from the atmosphere + sun with a box-downsampled roughness chain, recaptured on-demand only when the sun swings past ~6 degrees or the sky colours change, never per frame; a tolerance-parity approximation of the AtmosphereNpr dome on next, exact for the classic gradient sky). Colour / disabled skies have no environment and refuse honestly (`Docs/materials.md`)") \
	X(OutputGrade, "outputGrade", Asymmetric, "the shared output LOOK/GRADE stage: an authored contrast (S-curve) + saturation transform applied IDENTICALLY on both flavors (the shared curve is core_util/GradeMath) to the 3D scene only - the 2D tier (sprites/vector shapes/gui) is excluded so UI stays crisp, and the pass sequences LAST (after bloom when both are on). next = a CompositorManager2 grade quad after the scene pass; classic = the same curve as a viewport compositor over the generated-material scheme, the `OgreUnifiedShader.h` grade quad authored once and run in the GLSL ES 3.0 profile on a GLES/WebGL context (so it reaches the WebGL2/GLES3 web+device path too, gated on the glsl300es probe like bloom/IBL); the GLES2/WebGL1 floor answers false and an enabled grade degrades to no pass with one honest log line. DEFAULT OFF and byte-stable - content that never opts in renders byte-identically") \
	X(PlanarReflection, "planarReflection", Asymmetric, "opt-in MIRROR reflection of the actual scene (terrain + objects) in the water surface, rather than just the sky IBL cubemap it already samples: a camera reflected across the surface plane (normal +Y at `planeHeightY`) renders the scene into a reflection RenderTexture with the water surface hidden, which the water material samples at a FRESNEL-modulated blend (`reflectionStrength` boosts the base reflectivity and dims the body colour moderately - the surface stays water, never chrome). classic = the working path: a hand-authored GLSL program (desktop GL core + GLSL ES 3.0 variants, byte-identical bodies so web and desktop classic ripple alike) samples the mirror-camera RenderTexture at the fragment's ripple-perturbed screen UV (`screenUv + swellNormal.xz + disp`, so the mirror WOBBLES with the waves) with a Schlick fresnel + the sun's specular streak - reaching the WebGL2/GLES3 web+device path on the glsl300es probe; the GLES2/WebGL1 floor and a Vulkan/Metal context answer false pending their variant. next = the native planar-reflections subsystem: the hand-built reflection workspace renders the mirror RTT WITH its mip chain, starting ABOVE the sky queue and stopping below the water queue, and the mirror-on water datablock sets the env term's one angle-independent dial (a probe-calibrated mirror specular, near-mip-0 roughness, the squared body-dim law on albedo + scatter) that the classic program reads as the same three numbers, so both flavors evaluate ONE surface. Ogre's native planar reflection is a FLAT screen-projected mirror (the ripple normal only gates the plane distance/facing), so `createOrUpdateWaterDatablock` overrides the upstream `DoPlanarReflectionsPS` Hlms piece for water datablocks (a custom PS piece, parsed after the library so it wins - the swell VS piece's precedent) to ride the wave normal's slope and widen the harsh 20-degree tilt gate that would blank the mirror across crests - without it next's mirror reads glassy while classic ripples. the perturbation amplitude is calibrated so the reflected-OBJECT wobble measures at the classic mirror's strength (probe-equal on Mirror Lake's cube reflections). the mirror's sample sharpness is a LOD BAKED into that piece rather than read from the datablock's roughness, so the shared water roughness keeps shaping the sun glint's GGX lobe on both vignettes - the streak renders with and without the mirror, and the mirrorlake parity run gates its existence on both flavors. Both flavors compose the SAME lit body (the sun response + the hemisphere sky fill on the deep-colour albedo), the same scatter emissive, the same transmitted share at the authored opacity and the same roughness-aware fresnel, in linear light with one display transfer; with the mirror off the pair measures 7/8/3 on the open-water band. NEITHER mirror renders the sky DOME into its target, and that is a property of the TARGET rather than a per-flavor gap: the mirror is display-space LDR and the sky's radiance sits at or above display white across its whole extent, so a captured sky stores a colourless near-white (measured 7.4x the atmosphere's own background in linear light) and the surface reflects a milky neutral instead of the sky's colour. Both clear to the atmosphere's EVALUATED horizon colour instead - the one shared sky model answering what the sky is at the horizon - so the two mirror targets carry the same picture, within 3px on every reflected silhouette, and the open-water band measures 6 where two different skies left 25. The surface still receives the sky through its hemisphere ambient, as the non-mirror water does. The named open item on this scene is now the sun glint's ANGULAR reach: classic adds its Blinn streak as a raw additive term where next's is a fresnel-gated GGX lobe, so at the STEEP incidence this vignette's low, close camera creates the two peak alike (251 against 239 max luma) while classic holds 15.4% of the rock-mirror band above luma 150 against 0.7%; at the lake's grazing framing the pair agrees to 4/6/3. Probe-verified on both flavors by water_reflection_looks_right (strength) + water_mirror_wobble (the wave-perturbs-the-mirror EXISTENCE gate: it captures the same frame with and without the ORKIGE_WATER_FLAT_MIRROR diagnostic seam - which zeroes only the ripple UV perturbation - and asserts the mirror MOVES, a wall-clock-pacing-independent check; also the mirror diagnose seam ORKIGE_DUMP_MIRROR=<png>). Composes with screen-space refraction; off/unsupported keeps the byte-stable sky-reflection look") \
	X(SceneWireframeView, "sceneWireframeView", Asymmetric, "the editor Scene view's WIREFRAME polygon mode: the Scene view renders line-fill while every other target (Preview, camera inset, Play) stays solid, and the 2D/UI tier (the editor's own ImGui chrome, gui, sprites, vector shapes) stays solid too. Available on BOTH flavors, by DIFFERENT roads. classic = PER TARGET: the Scene RTT's OWN viewport camera flips to wireframe polygon mode (Camera::setPolygonMode, per-camera + leak-free, @see RenderTexture::setViewMode). next = a GLOBAL state under the one-game-view render invariant (@see RenderWorld::setSceneWireframe), because Ogre-Next bakes polygon mode into the pipeline state object with no per-pass/per-target override: the backend keeps the generated datablocks split into a 3D-SCENE set (PBS mesh/material/water) and a 2D/UI set (sprites/vector/lines/DrawLayer2D), and wireframe flips ONLY the scene set's macroblock polygon mode (byte-exact restore, PSO variant caches), armed by the editor only on a frame where the Scene view is the ONE game view rendering - the same dock-tab discipline SceneUnlitView uses, so it never leaks into the Preview / Play and composes with lighting-off (flat unlit wireframe). The solid+wireframe overlay (ShadedWireframe) is a SEPARATE capability that adds overlay items on top of the shaded pass rather than flipping polygon mode - @see SceneWireframeOverlayView") \
	X(SceneWireframeOverlayView, "sceneWireframeOverlayView", Asymmetric, "the editor Scene view's SHADED+WIREFRAME inspection look: the solid shaded scene with a thin wireframe drawn ON TOP of it (line edges over the lit surfaces). Available on BOTH flavors by the SAME road - OVERLAY ITEMS, never a mid-frame polygon-mode flip (impossible to bracket per-target on next's baked PSO, unbuilt as a second pass on classic). While armed (@see RenderWorld::setSceneWireframeOverlay), every scene-tier mesh instance gets a SECOND renderable sharing its mesh + node, drawn with ONE shared unlit near-black wireframe datablock/material whose macroblock carries polygon-mode wireframe + a small depth bias so the lines sit on the shaded surface without z-fighting; the shaded pass renders untouched, the overlay items add the lines. next = a second render Item per source item with the shared unlit wireframe datablock; classic = a second render Entity per source with a shared wireframe material. The overlays inherit the source node/transform, carry the editor-only visibility bit (masked OFF the Preview target, so they never leak into preview/Play), cast no shadows and are never pickable (query flags 0); the set rebuilds when the scene item set changes (live create/delete/mesh-swap) and disarm destroys it. ANIMATED/SKINNED meshes are excluded in this version (static scene geometry is the mode's habitat) - a skinned overlay would need its own animation state; soft-body/vector 2D content is scene-tier-excluded by construction. Radio-exclusive with the wireframe FLIP (SceneWireframeView) - only one is armed at a time, and both restore byte-exact") \
	X(SceneUnlitView, "sceneUnlitView", Asymmetric, "the editor Scene view's flat LIGHTING-OFF inspection look (albedo + a bright flat ambient, every analytic light's contribution removed). Available on BOTH backends as a GLOBAL per-frame state (@see RenderWorld::setLightingSuppressed), NOT per-target: a per-target route is impossible - Ogre-Next gathers directional lights from the scene's global light list, unfiltered by the per-pass light-visibility mask (OgreHlms.cpp 'Always gather directional & area lights'), and classic has no per-viewport lighting override at all. The editor sidesteps that by exploiting the DOCK-TAB layout: the Scene view and Preview are rarely visible together, so lighting-off is armed ONLY on a frame where the Scene view is the visible tab AND the Preview is not - a Scene-only frame, so a global flat look is correct (when both are visible the real game look wins and the Scene view stays lit). next = snapshot + hide every scene light + a flat-white hemisphere ambient; classic = the same over the scene lights + the per-pixel hemisphere-ambient sub-render-state and the flat term. RECOVER-THEN-REAPPLY (the ScreenShake precedent): releasing restores the ambient + every light's visibility EXACTLY, and the state flips on tab switches (not per frame) so the zero-light shader variant caches")

	//! the capability identities (expanded from ORKIGE_RENDER_CAPS)
	enum class RenderCaps
	{
#define ORKIGE_RENDER_CAP_ENUM(Ident, Name, Kind, Desc) Ident,
		ORKIGE_RENDER_CAPS(ORKIGE_RENDER_CAP_ENUM)
#undef ORKIGE_RENDER_CAP_ENUM
		Count	//!< sentinel: the number of capabilities
	};

	//! @brief the stable string name of a capability (matches the Lua/MCP keys
	//! and the matrix column). Returns "" for Count / out-of-range.
	inline char const * renderCapName(RenderCaps cap)
	{
		switch(cap)
		{
#define ORKIGE_RENDER_CAP_NAME(Ident, Name, Kind, Desc) case RenderCaps::Ident: return Name;
		ORKIGE_RENDER_CAPS(ORKIGE_RENDER_CAP_NAME)
#undef ORKIGE_RENDER_CAP_NAME
		case RenderCaps::Count: break;
		}
		return "";
	}

	//! @brief parse a capability name back to its enum; returns RenderCaps::Count
	//! (the invalid sentinel) for an unknown name.
	inline RenderCaps parseRenderCap(String const & name)
	{
		for(int each = 0; each < static_cast<int>(RenderCaps::Count); ++each)
		{
			RenderCaps const cap = static_cast<RenderCaps>(each);
			if(name == renderCapName(cap))
			{
				return cap;
			}
		}
		return RenderCaps::Count;
	}
}

#endif //__RenderCaps_h__16_7_2026__09_00_00__
